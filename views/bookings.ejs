<%- include('./layout/navbar') %>

<div class=" bg-color">
  <div class="container">
        <div class="d-flex flex-wrap  gap-5 py-5">
            <div>
                <img src="<%= movie.movieImage %>" height="400" width="300" class="" alt="...">
            </div>
            
                <div>
                    <h5 class="fs-3"><%= movie.movieName %></h5>

                    <p class="mb-0"><span class="fw-bolder"><%= movie.ratings %>\5</span> 
                        <span class="fs-5 text-warning"> &#9733;
                    </p>
                    <p class="mb-0 bg-white mt-2 pe-2"><span class="fw-bolder"></span> &nbsp;
                        <%= movie.movieType %>
                    </p><br>
                    <p class="mb-0 bg-white mt-2 pe-2"><span class="fw-bolder"></span> &nbsp;
                        <%= movie.movieGenre %>
                    </p>
                    <div class="mt-3"><%= movie.movieTami %><span>  •  </span><%= movie.movieGenre %><span ><span >  •  </span>UA16+<span >  •  </span><%= movie.movieDate %></div>
                    

                </div>

             </div>
         </div>
    </div>

    <form method="post" action="/reviewsCommit">
        <table border="0" align="center" cellpadding="10">
        <input type="hidden" name="id" value="<%= movie.id %>">
            <tr>
                <td>reviews</td>
                <td><input type="text" name="reviewsCommit" required></td>
            </tr>
            <tr>
                <td></td>
                <td><button title="submit">Submit Reviews</button></td>
            </tr>
        </table>
    </form>


    <div><%=  %> </div>